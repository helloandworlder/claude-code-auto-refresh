# Claude Code Agent Configuration
# 统一 Endpoint + 多个 API Key 配置

CLAUDE_ENDPOINT=https://claude-relay.example.com/api
CLAUDE_API_KEYS=your-api-key-1,your-api-key-2,your-api-key-3

# Optional: Log level
LOG_LEVEL=info

# 调度模式配置
# SCHEDULE_MODE: 调度模式，支持 'hourly' 或 'custom'
# - hourly: 24*7 每小时发送保活消息（默认模式）
# - custom: 自定义时间段模式
SCHEDULE_MODE=hourly

# 自定义模式配置（仅在 SCHEDULE_MODE=custom 时生效）
# CUSTOM_START_HOUR: 开始小时 (0-23)，默认 9
# CUSTOM_END_HOUR: 结束小时 (0-23)，默认 18  
# CUSTOM_WEEKDAYS: 工作日 (0=周日,1=周一,...,6=周六)，默认 1,2,3,4,5 (周一到周五)
CUSTOM_START_HOUR=9
CUSTOM_END_HOUR=18
CUSTOM_WEEKDAYS=1,2,3,4,5

# 自定义执行间隔（可选，单位：分钟，范围：5-1440，默认：60）
# INTERVAL_MINUTES: 设置保活消息的发送间隔
# - 默认值：60（每小时）
# - 示例：30（每30分钟）, 120（每2小时）, 15（每15分钟）
# INTERVAL_MINUTES=30
