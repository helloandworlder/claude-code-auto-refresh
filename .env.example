# Claude Code Agent Configuration
# 统一 Endpoint + 多个 API Key 配置

CLAUDE_ENDPOINT=https://claude-relay.example.com/api
CLAUDE_API_KEYS=your-api-key-1,your-api-key-2,your-api-key-3

# Optional: Log level
LOG_LEVEL=info

# Schedule Mode Configuration
# Mode: hourly (默认每小时触发) 或 custom (自定义时间区间)
SCHEDULE_MODE=hourly

# Custom Schedule Settings (当 SCHEDULE_MODE=custom 时生效)
# 开始时间 (HH:mm 格式)
SCHEDULE_START_TIME=09:00
# 结束时间 (HH:mm 格式，支持跨天，如 02:00 表示次日凌晨2点)
SCHEDULE_END_TIME=18:00
# 执行间隔 (分钟)
SCHEDULE_INTERVAL=30
# 可选：指定工作日 (1-7，1=周一，7=周日，用逗号分隔)
# SCHEDULE_WEEKDAYS=1,2,3,4,5
